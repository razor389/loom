# src/loom/config/__init__.py
"""
Configuration package.

Holds static project configuration artifacts (example settings, metrics catalog, mappings, ticker aliases).
At runtime, these files are loaded by the CLI/fetchers/domain validators to enforce the canonical contract.
"""
